{
  "name": "github-links",
  "displayName": "github-links",
  "description": "This extension opens up the github links in your browser",
  "version": "0.0.1",
  "publisher": "riteshsriv",
  "engines": {
    "vscode": "^1.37.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:githublinks.history",
    "onCommand:githublinks.page"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "githublinks.history",
        "title": "Open File GitHub History"
      },
      {
        "command": "githublinks.page",
        "title": "Open File GitHub Page"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/git-branch": "^2.0.1",
    "@types/glob": "^7.1.1",
    "@types/lodash": "^4.14.137",
    "@types/mocha": "^5.2.6",
    "@types/node": "^10.12.21",
    "@types/voca": "^1.4.0",
    "@types/vscode": "^1.37.0",
    "glob": "^7.1.4",
    "mocha": "^6.1.4",
    "tslint": "^5.12.1",
    "typescript": "^3.3.1",
    "vscode-test": "^1.0.2"
  },
  "dependencies": {
    "git-branch": "^2.0.1"
  }
}
